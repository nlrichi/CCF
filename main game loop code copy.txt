#screen.blit(main_background,(0,0)) 
    #if game_running:
        #bird_movement += gravity
        #bird_rect.centery += bird_movement
        #screen.blit(game_bird,bird_rect) 
 
        #pillar_list = move_pipe(pillar_list)
        #draw_pipes(pillar_list) 
        #if delay_counter>=83:
            #if score_timer == (2*60) and check_collisions(pillar_list)==True:
                #game_score +=1
                #score_timer = 0
            #else:
                #score_timer += 1
        #else:
            #delay_counter+=1
        #display_score('main_game') 
    #else:
        #main_menu()
        #gamehigh_score = high_score(gamehigh_score,game_score)
        #display_score('game_over')   

    #soil_xpos-=1
    #build_floor() 
    #if soil_xpos <= -900:
        #soil_xpos =0
    #game_running= check_collisions(pillar_list)
    #pygame.display.update() #keeps the window on the screen infinately
    #clock.tick(60)